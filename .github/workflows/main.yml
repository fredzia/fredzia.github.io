name: create-html
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run build script
      run: |
         chmod +x ./scripts/build.sh
         ./scripts/build.sh
      shell: bash
